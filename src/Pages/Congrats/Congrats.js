import { Link } from 'react-router-dom';

export default function Congratulations() {
    //const { type } = useParams();
    return (
        <div>
            <h1>wasd</h1>
            <Link to={'/'}>Set New Habit</Link>
        </div>
    );
}
