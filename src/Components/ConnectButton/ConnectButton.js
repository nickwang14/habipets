const ConnectButton = () => (
    <button
        className="ConnectButton"
        onClick={() => {
            alert('Altura Connect wallet backend');
        }}
    >
        Connect to play
    </button>
)

export default ConnectButton